# Test-only dependencies. The app's real runtime deps live in requirements.txt.
# Install both inside the image to run the suite:
#   pip install -r requirements.txt -r requirements-dev.txt && pytest
pytest>=8.0
pytest-asyncio>=0.24
mypy>=1.8
