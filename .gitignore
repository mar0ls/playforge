.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
data/
!data/.gitkeep

# backup archives — these hold the credential master key and your project repos.
# Never commit one. (scripts/backup.sh writes here by default.)
backups/
*.tar.gz.gpg
.DS_Store
.idea/
.vscode/
TODO.md
examples/*.zip


# local secrets/config — keep the template (.env.example) tracked
.env

# local example playbooks used for live testing (not part of the app)
playbooks/

# heavy design sources — only the optimized derivatives are committed
# (assets/hero.jpg, static/img/favicon.png, static/img/mark.png)
assets/logo1.png
assets/background_p.png
